// Aggregated content stats patterns
export const CONTENT_STATS_PATTERNS = {
  // Get all content stats for a single doctor (reviews + answers + blogs)
  ALL_BY_DOCTOR: 'content.stats.allByDoctor',

  // Get paginated & sorted list of all doctors' content stats
  GET_DOCTORS_LIST: 'content.stats.getDoctorsList',
};
